extends layout 
block content 
    h1 This is the minimal Register Page
    form(action="/register" method="POST")
        label(for="email") Enter your email 
        input("name"="email")
        div 
        label(for="password") Enter your password: 
        input("name"="password")
        div 
        input(type="submit" value="Register")